/**
 * @file Implements marital status enum to specifiy a
 * type of marital status.
 */


enum MaritalStatus {
    Married = 'MARRIED',
    Single = 'SINGLE',
    Widowed = 'WIDOWED'
};

export default MaritalStatus;